<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title></title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
    <div class="wrapper">
        <header>
            <h1>
                <a class="logo" href="/">Speaking Tech</a>
            </h1>
            <nav>
                {{#if loggedIn}}
                <a class="nav-elem" href="/dashboard">DashBoard</a>
                <button id="logout" class="btn-no-style nav-elem">LogOut</button>
                {{else}}
                <a class="nav-elem" href="/login">LogIn</a>
                {{/if}}
            </nav>
        </header>
        <main>
            {{{ body }}}
        </main>
        <footer>
            Free Speech Platform for Technology
        </footer>
    </div>

    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
</body>

</html>